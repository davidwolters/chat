import styled from 'styled-components';

// Container taking up 5/7 parts of the screen.
const MessagesContainer = styled.div`
	order: 1;
	flex: 5;
	padding: 10px 5px;
	overflow-y: scroll;
`;

export default MessagesContainer;